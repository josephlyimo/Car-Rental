<h2>{{car.name}}</h2>
<p>Type: {{car.type}}</p>
<p>Color: {{car.color}}</p>
<p>Status: {{car.status}}</p>
<div class="car-card" style="max-width: 600px;">
  <h3>Description</h3>
  <p style="white-space: pre-wrap;">{{car.description}}</p>
</div>

<p>Price {{car.price}} For {{car.base_rental_duration}} days</p>
<p>Note: If booking exceeds {{car.base_rental_duration}} days, price increases by 20,000 per extra day.</p>

{{#if user}}
  <form action="/bookings/new" method="GET">
    <input type="hidden" name="car_id" value="{{car.id}}" />
    <button type="submit" class="btn">Proceed to Booking</button>
  </form>
{{else}}
  <p><a href="/users/login">Login</a> to proceed with booking.</p>
{{/if}}
