<h2>Admin Profile</h2>
<p>Welcome, {{user.name}}</p>

{{#if user.profileImage}}
  <img src="/uploads/profiles/{{user.profileImage}}" alt="Profile Image" style="width: 150px; height: 150px; border-radius: 50%; object-fit: cover;" />
{{else}}
  <p>No profile image uploaded.</p>
{{/if}}

<p><a href="/admin/profile/edit" class="btn btn-primary" style="margin-top: 10px; display: inline-block;">Edit Profile Image</a></p>
